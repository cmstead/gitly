function branchConstants () {

    return {
        'Create a branch (--add)': 'add',
        'Check out a branch (--checkout)': 'checkout',
        'List all branches (--list)': 'list'
    };
}

module.exports = branchConstants;